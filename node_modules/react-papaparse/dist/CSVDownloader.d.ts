import React from 'react';
import { ParseConfig } from 'papaparse';
export declare const LINK_TYPE = "link";
export declare const BUTTON_TYPE = "button";
export interface Props<T> {
    children: React.ReactNode;
    data: any;
    filename: string;
    type?: 'link' | 'button';
    style?: any;
    className?: string;
    bom?: boolean;
    config?: ParseConfig<T>;
}
export default class CSVDownloader<T = any> extends React.Component<Props<T>> {
    static defaultProps: Partial<Props<unknown>>;
    download: (data: any, filename: string, bom: boolean, config: ParseConfig<T>) => void;
    render(): React.ReactNode;
}
